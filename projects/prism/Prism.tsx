/** @format */

import Camera from "./Camera"
import * as S from "./UI"

/** un seul bloc : la camera affichee se choisit dans le panneau */
export const Prism = () => (
	<S.Container>
		<Camera />
	</S.Container>
)
